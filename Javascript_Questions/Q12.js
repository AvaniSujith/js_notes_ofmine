// Compute the perimeter and area of a rectangle.

function rectangle(l,b){
    let perimeter = 2*(l+b);
    let area = l * b;
    console.log(`Area of triangle is ${area} and perimeter is ${perimeter}`)
}

rectangle(15,6);
rectangle(25,10);
rectangle(18,9);