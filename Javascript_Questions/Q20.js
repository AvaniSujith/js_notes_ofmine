// Calculate the length of a hypotenuse in a right triangle given the other two sides.

function length(a,b){
    let c = a**2 + b**2;
    console.log(`The length of hypotenuse is ${c}`);
}

length(2,8)

